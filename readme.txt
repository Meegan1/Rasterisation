Setup:
    Run the following commands to compile and run:
        -   make
        -   ./a.out


QUESTION F:
    The location when the texture coordinates are rounded to 6 decimal places is Canada Place (Port of Vancouver).